$(document).ready(function(){
  var $container = $('#my-posts');
  $container.masonry({
    itemSelector: '.post-box',
    columnWidth: function( containerWidth ) {
      return containerWidth / 2;
    }
  });
  $('.get-to-comment').each(function(){
    var id = $(this).attr('value');
    $(this).click(function(){
      $('.comment-box-' + id).focus();
    });
  });
});
