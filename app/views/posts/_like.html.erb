<%= form_for like, remote: true do |f| %>
  <div><%= hidden_field_tag :post_id, post.id %></div>
  <%= f.submit type: "image", src: image_path("resources/like.png"),
    class: "like-button", value: "" %>
<% end %>
