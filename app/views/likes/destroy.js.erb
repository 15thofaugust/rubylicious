$('.post-box-like-<%= @post.id %>')
  .html('<%= j render partial: "posts/like", locals: {post: @post}%>');
$('.single-post-like-<%= @post.id %>')
  .html('<%= j render partial: "posts/like", locals: {post: @post} %>');
$(".likes-count-<%= @post.id %>").html('<%= @post.likes.count %>');
